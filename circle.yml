checkout:
  post:
    - git submodule sync
    - git submodule update --init

machine:
  services:
    - docker
  java:
    version: openjdk7

general:
  artifacts:
    - "C:\Users\fara\Documents\GitHub\ProjectSCM\SCMProject\src"