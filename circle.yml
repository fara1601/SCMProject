checkout:
  post:
    - git submodule sync
    - git submodule update --init

machine:
  java:
    version: oraclejdk8

general:
  artifacts:
    - "build/libs/*.jar"
    - "build/distributions/*.zip"
